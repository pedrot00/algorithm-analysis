Função | Comentário

push_front() | Insere elemento no início da lista
pop_front() | Remove o primeiro elemento
size() | Retorna o número de elementos (você já pode usar a variável dataSize)
empty() | Retorna se a lista está vazia

find(const T& val) | Procura por um elemento (retorna ponteiro ou índice)
remove(const T& val) | Remove o primeiro nó com determinado valor
at(int index) | Retorna o valor no índice (cuidado: acesso é O(n))
reverse() | Inverte a ordem dos elementos da lista
copy() | Copia outra lista para esta (importante pra cópia profunda)